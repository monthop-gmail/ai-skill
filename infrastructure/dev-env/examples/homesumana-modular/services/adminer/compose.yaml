# ─── Adminer — DB management UI ─────────────────────────

services:
  adminer:
    image: adminer
    depends_on:
      db:
        condition: service_healthy
    networks:
      - homesumana-net
    restart: unless-stopped
