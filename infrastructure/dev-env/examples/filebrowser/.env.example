# FileBrowser
PUID=1000
PGID=1000

# โฟลเดอร์ที่จะเปิดให้เข้าถึง (path บน host)
DATA_DIR=./data

# Cloudflare Tunnel
# สร้าง Tunnel บน CF Dashboard → Zero Trust → Networks → Tunnels
# ตั้ง Public Hostname: files.dev.example.com → http://filebrowser:80
CF_TUNNEL_TOKEN=
