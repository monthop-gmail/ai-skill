# Production override
# ใช้: docker compose -f docker-compose.yml -f docker-compose.prd.yml up -d

services:
  minio:
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
